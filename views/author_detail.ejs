<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <%- include('layout'); -%>
        <div class="col-sm-10">
          <h1>Author: <%= title %></h1>
          <h5><%= author.dob_dod_formatted %></h5>
          <div style="margin-left: 20px; margin-top: 20px">
            <h6 style="font-size: 1.5rem; margin-bottom: 1rem">
              List of works:
            </h6>
            <% if (authorBooks.length) { %> <% for (let i = 0; i <
            authorBooks.length; i++){ %>
            <strong style="margin-bottom: 0rem">
              <a href="<%= authorBooks[i].url %>"
                ><%= authorBooks[i].title %></a
              >
              (IBSN: <%= authorBooks[i].isbn %>)
            </strong>
            <p><%= authorBooks[i].summary %></p>
            <% } %> <% } else { %>
            <p>There are no books for this author.</p>
            <%} %>
            <hr />
          </div>
          <p>
            <a href="<%= author.url%>/delete">Delete author</a
            >&nbsp;&nbsp;|&nbsp;&nbsp;<a href="<%= author.url%>/update"
              >Update author</a
            >
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
