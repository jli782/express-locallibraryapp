<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <%- include('layout'); -%>
        <div class="col-sm-10">
          <h1><%= title %></h1>
          <ul>
            <%for (let i = 0; i < authors.length; i++){ %>
            <li>
              <a href="<%= authors[i].url %>"><%= authors[i].name %> </a
              >(<%=authors[i].dob_dod_formatted %>)
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </body>
</html>
