<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <%- include('layout'); -%>
        <div class="col-sm-10">
          <h1><%= title %>: <%= book_instance._id %></h1>
          <p>
            <strong>Title: </strong
            ><a href="<%=book_instance.book.url%>"
              ><%= book_instance.book.title %></a
            >
          </p>
          <p><strong>Imprint: </strong><%= book_instance.imprint %></p>
          <p>
            <strong>Status: </strong>
            <% if (book_instance.status == 'Available') { %>
            <span class="text-success"><%= book_instance.status %></span>
            <% } else if (book_instance.status == 'Maintenance') {%>
            <span class="text-danger"><%= book_instance.status %></span>
            <% }else { %>
            <span class="text-warning"><%= book_instance.status %></span>
            <% }%>
          </p>
          <p>
            <strong>Due back: </strong><%= book_instance.due_back_formatted %>
          </p>
          <p>
            Do you really want to delete this book instance <%=
            book_instance._id %>?
          </p>
          <form method="post">
            <div class="form-group">
              <input
                id="bookinstanceid"
                class="form-control"
                type="hidden"
                name="bookinstanceid"
                value="<%= book_instance._id %>"
              />
            </div>
            <button class="btn btn-primary" type="submit">Delete</button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
