<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <%- include('layout'); -%>
        <div class="col-sm-10">
          <h1>Genre: <%= genre.name %></h1>
          <p><a href="<%= genre._id%>/delete">Delete Genre</a></p>
          <hr />
          <div style="margin-left: 20px; margin-top: 20px">
            <h2 style="font-size: 1.5rem">Books</h2>
            <% if (relatedbooks.length) {%> <%for (let i = 0; i <
            relatedbooks.length; i++){ %>
            <strong>
              <a href="<%= relatedbooks[i].url %>"
                ><%= relatedbooks[i].title %></a
              >
            </strong>
            <p style="margin-left: 0.25rem"><%= relatedbooks[i].summary %></p>
            <%} %> <% }else {%>
            <p style="margin-left: 0.25rem">This genre has no books.</p>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
